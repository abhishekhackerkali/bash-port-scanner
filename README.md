# Bash Port Scanner 🔍

A simple Bash script to scan open ports on a given host.

## Features
- Scan a range of ports
- Lightweight & fast
- No external tools required

## Usage
```bash
chmod +x port_scanner.sh
./port_scanner.sh <host> <start_port> <end_port>
